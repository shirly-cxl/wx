<!--pages/demo/mapDemo/mapDemo.wxml-->
<view>
 <map class="mapSize"  id="myMap" latitude="{{markers[0].latitude}}" longitude="{{markers[0].longitude}}"
  markers="{{markers}}" ></map> 
  <button bindtap="chooseLocation">选择位置</button>
  
</view>
